import { FC } from 'react';
import HomeView from './HomeView';

/**
 * Homeページ/container
 */
const HomeContainer: FC = () => {
  return (
    <div>
      homeContainer
      <HomeView />
    </div>
  );
};

export default HomeContainer;
